/**
 * Commercial settings for the Pricing page. Leave a value empty until it is decided:
 * the page then shows "coming soon" instead of a price, and hides the matching FAQ answer.
 */
export const PRICING = {
  /** Display string, e.g. "99.000đ". */
  monthlyPrice: "",
  yearlyPrice: "",
  /** Percent saved by paying yearly, e.g. 30. 0 hides the badge. */
  yearlySavePercent: 0,
  /** Free-plan test allowance, e.g. "2 đề luyện mẫu". Empty hides the line. */
  freeAllowance: "",
  /** Where "Upgrade to Pro" goes until checkout exists (a checkout URL or a contact form). */
  upgradeUrl: "",
  /** Contact address for the classes / schools plan. */
  contactEmail: "",
  paymentMethods: "",
  newTestsPolicy: "",
};
